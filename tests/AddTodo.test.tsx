import { render, screen } from '@testing-library/react';
import AddToDo from '../src/components/AddToDo';

describe('AddToDo', () => {
  it('opens a modal when clicking in a placeholder ToDo', () => {});
});
